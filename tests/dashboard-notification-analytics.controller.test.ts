import { afterEach,beforeEach,describe,expect,mock,spyOn,test } from 'bun:test';
import { dashboardController } from '../src/controller/dash';
import Admin from '../src/models/admins.model';import sessionService from '../src/services/session.service';import { signAccessToken,TokenAudienceEnum } from '../src/constants/jwt';import { IUserRoleEnum } from '../src/interfaces/user.interface';import { IAdminPermissionEnum } from '../src/interfaces/admin.interface';
const uid='507f191e810c19729de86401',sid='12345678-1234-4234-8234-123456789012',id='507f191e810c19729de86402';const chain=(value:any)=>({select(){return this},lean(){return this},exec:async()=>value});
function req(path:string,role?:any,audience:any=TokenAudienceEnum.DASHBOARD){const headers:any={};if(role)headers.authorization=`Bearer ${signAccessToken({_id:uid,role,sid,audience})}`;return new Request(`http://localhost${path}`,{headers})}
describe('Dashboard notification analytics contracts',()=>{beforeEach(()=>{process.env.ACCESS_TOKEN_SECRET='test-access-secret-that-is-long-enough';spyOn(sessionService,'validateAccess').mockImplementation(async(p:any)=>({sid:p.sid,userId:p._id,role:p.role,audience:p.aud,restricted:false} as any))});afterEach(()=>mock.restore());test('guards routes before analytics access and validates malformed ids',async()=>{const path='/dash/admin/notifications/not-an-id/analytics';expect((await dashboardController.handle(req(path))).status).toBe(401);expect((await dashboardController.handle(req(path,IUserRoleEnum.PATIENT,TokenAudienceEnum.MOBILE))).status).toBe(401);const admin=spyOn(Admin,'findOne').mockReturnValue(chain({user_id:uid,is_active:true,super_admin:false,permissions:[]}) as any);expect((await dashboardController.handle(req(path,IUserRoleEnum.ADMIN))).status).toBe(403);admin.mockReturnValue(chain({user_id:uid,is_active:true,super_admin:false,permissions:[IAdminPermissionEnum.MANAGE_SETTINGS]}) as any);expect((await dashboardController.handle(req(path,IUserRoleEnum.ADMIN))).status).toBe(400)});test('generated route surface includes all analytics operations',()=>{for(const suffix of ['analytics','readers','recipients','unread-recipients'])expect(dashboardController.routes.some(r=>r.path===`/dash/admin/notifications/:id/${suffix}`)).toBe(true)})});
